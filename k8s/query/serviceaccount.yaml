apiVersion: v1
kind: ServiceAccount
metadata:
  name: retrieval-api
  namespace: query
  labels:
    azure.workload.identity/use: 'true'
  annotations:
    # Rendered into build/ by scripts/render-k8s.sh from terraform outputs.
    azure.workload.identity/client-id: __RETRIEVAL_API_CLIENT_ID__
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: mcp-server
  namespace: query
  labels:
    azure.workload.identity/use: 'true'
  annotations:
    azure.workload.identity/client-id: __MCP_SERVER_CLIENT_ID__
